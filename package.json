{
  "name": "axiomid-knowledge-base",
  "description": "AIX Sovereign Stack — Knowledge Base & Wikigraph",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "build": "rm -rf quartz/content && cp -R wiki quartz/content && cd quartz && npm run quartz build",
    "dev": "rm -rf quartz/content && cp -R wiki quartz/content && cd quartz && npm run quartz build --serve",
    "update-graph": "python3 scripts/update-wiki.py",
    "validate": "python3 scripts/validate-links.py"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/AIX-Format/axiomid-knowledge-base.git"
  }
}
